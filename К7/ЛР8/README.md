Задание 1. Найти все пары операций, сумма которых превышает порог (без повторов).
Алгоритмический шаблон: two pointers (через сортировку копии)
Тестовые данные:
operations = [2, 7, 11, 15]
threshold = 9
operations = [1, 2, 3]
threshold = 10
operations = [5, 5, 5]
threshold = 9
operations = [4, 6]
threshold = 10

Бизнес логика проекта: Если sum(left, right) > threshold, значит, текущий элемент right образует подходящую пару со всеми элементами от leftдоright -1 Мы добавляем эти пары в результат и сдвигаем right влево.Если сумма меньше или равна порогу, сдвигаем left вправо, чтобы увеличить сумму.
Задиние 2. Финансовые расходы Что означают данные • число = расходы за день. Генерация expenses = [random.randint(10, 200) for _ in range(30)] Обработка • минимальный период, где сумма ≥ лимита.

Бизнес логика проекта: Этот проект направлен на симуляцию банковского мониторинга, где необходимо выявить комбинации транзакций (пары), общая сумма которых превышает определенный лимит.
